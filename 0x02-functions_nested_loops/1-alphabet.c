#include <stdio.h>

/**
 * print_alphabet - prints lower case alphabet, followed by a new line
 */
int main(void)
{
char ch;
ch = 'a';
while (ch <= 'z')
{
putchar(ch);
ch++;
}
putchar('\n');
Return (0);
}
